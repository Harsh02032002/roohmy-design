kind = "web"
previewPath = "/"
title = "Roomhy Website"
version = "1.0.0"
id = "artifacts/roomhy-website"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 22789

[services.development]
run = "pnpm --filter @workspace/roomhy-website run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/roomhy-website", "run", "build" ]
publicDir = "artifacts/roomhy-website/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "22789"
BASE_PATH = "/"
